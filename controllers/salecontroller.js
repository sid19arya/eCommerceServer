function generateSaleReport(items, sum, discounts){
    // items is an object with key value pairs of prods and items
    // sum is the total raw amount
    // discounts are any discounts to be applied
}

module.exports = {generateSaleReport}